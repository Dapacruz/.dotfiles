#!/usr/bin/env bash


configs=(
    "$HOME/.zshrc"
    "$HOME/.zsh.funcs"
    "$HOME/.zsh.d"
    "$HOME/.local/bin"
    "$HOME/.config/starship.toml"
    "$HOME/.config/kitty"
    "$HOME/.config/tmux"
    "$HOME/.config/lazygit"
)

common_configs=(
    "$HOME/.config/nvim"
)
