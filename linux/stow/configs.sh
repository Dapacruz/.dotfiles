#!/usr/bin/env bash


configs=(
    "$HOME/.config/nvim"
    "$HOME/.local/bin"
    "$HOME/.zsh.d"
    "$HOME/.zsh.funcs"
    "$HOME/.zshrc"
)

common_configs=(
    "$HOME/.config/git"
    "$HOME/.config/kitty"
    "$HOME/.config/lazygit"
    "$HOME/.config/starship.toml"
    "$HOME/.config/tmux"
)
