#!/usr/bin/env bash

configs=(
    "$HOME/.zshrc"
    "$HOME/.config/starship.toml"
    "$HOME/.config/kitty"
    "$HOME/.config/nvim"
    "$HOME/.config/tmux"
    "$HOME/.config/raycast"
    "$HOME/.config/cheat"
    "$HOME/.config/lazygit"
    "$HOME/.config/karabiner"
    "$HOME/.config/lf"
    "$HOME/.config/neofetch"
    "$HOME/.zsh.funcs"
    "$HOME/.zsh.d"
    "$HOME/.local/bin"
)
