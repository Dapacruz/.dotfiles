#!/usr/bin/env bash

configs=(
    "$HOME/.config/cheat"
    "$HOME/.config/karabiner"
    "$HOME/.config/lf"
    "$HOME/.config/neofetch"
    "$HOME/.config/raycast"
    "$HOME/.local/bin"
    "$HOME/.zsh.d"
    "$HOME/.zsh.funcs"
    "$HOME/.zshrc"
)

common_configs=(
    "$HOME/.config/git"
    "$HOME/.config/kitty"
    "$HOME/.config/lazygit"
    "$HOME/.config/nvim"
    "$HOME/.config/starship.toml"
    "$HOME/.config/tmux"
)
