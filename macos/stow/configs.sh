#!/usr/bin/env bash

configs=(
    "$HOME/.zshrc"
    "$HOME/.config/kitty"
    "$HOME/.config/nvim"
    "$HOME/.config/tmux"
    "$HOME/.config/raycast"
    "$HOME/.config/cheat"
    "$HOME/.config/karabiner"
    "$HOME/.config/lf"
    "$HOME/.config/neofetch"
    "$HOME/scripts"
    "$HOME/.config/starship.toml"
)
