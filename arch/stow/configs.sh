#!/usr/bin/env bash

configs=(
    "$HOME/.config/btop"
    "$HOME/.config/hypr"
    "$HOME/.config/kanata"
    "$HOME/.config/kanshi"
    "$HOME/.config/kitty"
    "$HOME/.config/rofi"
    "$HOME/.config/starship.toml"
    "$HOME/.config/systemd"
    "$HOME/.config/waybar"
    "$HOME/.local/bin"
    "$HOME/.zsh.d"
    "$HOME/.zsh.funcs"
    "$HOME/.zshrc"
)

common_configs=(
    "$HOME/.config/git"
    "$HOME/.config/lazygit"
    "$HOME/.config/nvim"
    "$HOME/.config/tmux"
)
