#!/usr/bin/env bash


configs=(
    "$HOME/.config/kitty"
    "$HOME/.local/bin"
    "$HOME/.config/starship.toml"
    "$HOME/.zsh.d"
    "$HOME/.zsh.funcs"
    "$HOME/.zshrc"
)

common_configs=(
    "$HOME/.config/git"
    "$HOME/.config/lazygit"
    "$HOME/.config/nvim"
    "$HOME/.config/tmux"
)
